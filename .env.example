# CareGrid.io frontend environment variables
# Copy to .env.local and adjust as needed.

# Base URL of the Spring Boot REST API (future integration).
# Leave unset while the app runs on the built-in mock data layer.
# VITE_API_BASE_URL=http://localhost:8080/api